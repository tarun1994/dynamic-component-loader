import { ProfileHostDirective } from './profile-host.directive';

describe('ProfileHostDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfileHostDirective();
    expect(directive).toBeTruthy();
  });
});
